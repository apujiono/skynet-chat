export async function encryptMessage(message, key) {
  return message; // Placeholder, implementasi E2EE di production
}

export async function decryptMessage(encrypted, key) {
  return encrypted; // Placeholder
}
