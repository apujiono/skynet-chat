[phases.setup]
nixPkgs = ['python310', 'python3']
aptPkgs = ['build-essential', 'libpq-dev']
cmds = ['pip install --no-cache-dir -r requirements.txt']

[phases.build]
cmds = ['python -m compileall .']

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
